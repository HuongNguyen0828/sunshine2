# This folder include all of:

- Shared code (Types, API utils)
- Shared code (Types + DTOs only)

# Set up Shared

cd shared
expo install expo-constants
npm install firebase
npm install --save-dev @types/firebase

## or with yarn

yarn add firebase
yarn add -D @types/firebase
